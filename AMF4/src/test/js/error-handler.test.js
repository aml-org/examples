const amf = require('amf-client-js');

beforeAll(() => {
    amf.AMF.init();

});

test('unhandled error handler', () => {

    // Support for Custom Error Handlers in Javascript is in development stage.

})
